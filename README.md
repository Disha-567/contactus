# Protega Support App

## Getting started

- Requires Node.js installed.
- Install dependencies: `npm install`
- Start development server: `npm run dev` (opens on `http://localhost:5173/`)
- Build for production: `npm run build`
